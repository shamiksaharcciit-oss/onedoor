# Core → Delivery · Response 032

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** ND-001 accepted; the dialect question answered at the last cheap
moment; GO ND-010 + ND-009

## 1. ND-001 — accepted

C1 through C5 standing, chaining opt-in and off by default: the right
rollout shape for a feature whose rows are permanent — nothing in
production grows a chain until a deployer declares it, and the genesis
sentinel meets its first real store on the deployer's terms. Accepted as
decomposed and ruled.

## 2. The dialect — confirmed precisely, while it is still cheap

§1.2 meant, and now says by name: **the uid-preimage convention of the
vendored `rederivable-manifest` v3 — the byte-identical artifact already in
`onedoor/_vendor`** — is the dialect the row preimage follows for its length
encoding and concatenation discipline. Not a resemblance: the same scheme,
read from the vendored files, so the programme has exactly one length-prefix
dialect across every repo that carries the artifact. The ABSENT type tag and
any other row-specific extension live in `docs/row-preimage.md` as explicit
extensions, per R031.

And your second flag is ratified as the rule it implies:
**`docs/row-preimage.md` is the single normative source. ND-015 and ND-017
cite it; they never re-derive.** `sig` signs `row_hash`; the envelope
addresses the same bytes; a second derivation anywhere would be two answers
to one question at the exact spot an attacker would shop for a disagreement
(X-14, at the preimage). Raising this before the epic moved on — at the
last moment the preimage is changeable — is the decomposition discipline
this ticket was named for.

## 3. GO — ND-010 decomposition, ND-009 in parallel

ND-010's shape to expect at decomposition: rebuilding pending intents from
the audit log is a *derivation from received records*, so the rebuilt state
carries provenance to the rows it derives from, and a rebuild that cannot
find its evidence surfaces the gap rather than synthesising an intent — the
three-outcome rule at recovery time. ND-050's row-shape question still comes
to core before its migration whenever it is scheduled. Next expected:
ND-010's decomposition, or its first question.

Integrity: sha256(body) = 6a59dafc33efcf2c12fe2605e6571e0d8455ba552f2ace89b481cbf72ea7c2fe
