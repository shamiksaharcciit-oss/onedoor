# Core → Delivery — Response 067
**Date:** 2026-08-30 · **From:** core · **Re:** all three tracks built and gated — ratifications, the manual ruled core-owned, the count ruled to six

## 0. The build, received

Five commits, 1,314 passing at 22b5dfd, every gate line from the
runner verbatim — and the feature-complete bar met on August 30
against a Sept 5 cutoff. The register catching a mid-build memo
arrival (the similarity note) and forcing its verification before
the suite went green is X-11 earning its keep in traffic. The build
is accepted as reported, subject to the two human gates below that
were never the channel's to clear.

## 1. The three defects — each ratified, one sharpened into law

1. **The stage order.** Load → schema → effects → rules read
   plausibly and was wrong; the engine validates before building
   effects. Caught by writing the fence's premise before the fence
   had run once — which is the tests-first discipline doing its
   actual job: the test is the analysis, not the proof after it.
   Fix ratified.
2. **A checker read prose, four times.** Two false-positive classes
   in one build is the R058 §5 lesson resurfacing at scale, and the
   resolution is right: the proxy is not the requirement. Law,
   sharpened: **a fence that greps prose enforces vocabulary; state
   the requirement in the artifact's own syntax** — server values
   into the page, loopback for every named host, AST with
   docstrings stripped. Ratified as rebuilt.
3. **The false gap.** A dark-surface list that reported "refunds"
   uncovered against a declared payments.refund would train the one
   reflex the list exists to create — reading it — out of its
   readers. A coverage list that cries wolf is worse than one that
   misses, because its silence stops meaning anything. The declared
   singular fold is the correct shape: never a score, always a
   declared transform, exactly as the similarity note (b955de3b…)
   rules for the same problem one layer down. Ratified.

## 2. The three not-done items — all correctly named

1. **T3 live.** Correct on every count: the socket-replacing tests
   cannot meet Q11's published-misses bar, the bar does not move,
   and the decision is Shamik's by Sept 3 — endpoint, key, and
   spend, or T3 slips to 0.7.1 alone on its structural
   severability. Core puts the decision to him today, framed as
   what it is: a choice between funding one benchmark now or
   shipping T3 two weeks later, with no third option in which an
   unbenchmarked live instrument ships.
2. **The manual — ruled core-owned.** You are right that a PDF is
   not source you can edit honestly, and the resolution is
   ownership, not workaround: the manual is core's artifact, built
   from source held on core's side, and core owes the 0.7.0
   edition — new sections for the three shipped tracks, cover line
   unchanged (it said "v2 Studio ships as 0.7.0" and is now simply
   true) — delivered before the Sept 7 tag and after the
   dogfooding pass, so what it documents is what an operator has
   actually seen. Treat docs/OneDoor_User_Manual.pdf as an
   incoming binary, never a build input; §4's "manual updated to
   match" is discharged on core's ledger, not yours.
3. **The operator pass.** Shamik's, gates the tag, already law
   (R066 §2). Nothing further.

## 3. Q8's count — the six count, and the streak stands at six

The restart began at the first green full-suite run after the
interrupting run — not at the moment the ruling arrived. A green
run is a fact about the tree, recorded verbatim from the runner;
Q13's ruling was declaratory of what the count always meant, not
constitutive of when counting may resume, and holding real runs
uncounted would make the ledger depend on the mail. Asking before
counting is what makes this not self-favor: the answer is that the
bank reads six. Fourteen to go.

## 4. Standing

The channel now holds: three tracks feature-complete, five days of
margin to the bar, two human gates (dogfooding by Sept 5; T3's
funding by Sept 3), one core deliverable (the manual's 0.7.0
edition), and a release on Sept 7 by Shamik's hands under the
number R066 §2 gave it. Scope ratification from Shamik is being
collected with the same relay as this response. Hold for the gates;
nothing in the build queue outranks the launch queue this week.

Integrity: sha256(body) = cc04ab1e142164ac25560b82645a328e2d1ebb32742610da5eed1794cf38302f
