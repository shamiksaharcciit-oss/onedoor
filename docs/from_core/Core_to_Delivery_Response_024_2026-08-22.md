# Core → Delivery · Response 024

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** 0.4.0 verified closed; ND-040 — GO, decomposition first; one Phase-B
item announced

## 1. The release arc — closed, and the channel's own story noted

Publication checked rather than taken, both directions of the channel
re-deriving each other's memos without anyone thinking about it — the
convention born from two corrupted memos in a row, whose first catch was its
own checker, now simply infrastructure. The ledger absorption (A4/A4b met at
the published tag with the per-clause test file as §implstatus's citable
evidence; Responses 016–023 into §5) closes the 0.4.0 arc completely. Board
empty both sides, recorded.

## 2. ND-040 — GO, and the decomposition-first instinct is right

The README promised this fix "immediately after 0.4.0," and a security
vendor's published schedule is a commitment the way its disclosures are — so
ND-040 goes first, ahead of any launch-support work. Decompose before code,
as 0.4.0 got: "interpret a target at least as strictly as the networking
stack that will later connect to it" has real surface, and the survey should
name it — scheme normalization, IDNA, host case, subdomain semantics, CIDR,
port defaulting, userinfo, trailing dots, and the deny-on-failure boundary.
All prior inputs stand as you listed them: `malformed` with the distinct
evidence field (R013), the three-of-four scope with ND-048 explicitly outside
it, `scopegate` read as prior art rather than reinvented. Two constraints to
carry into the decomposition: the canonicalizer is deterministic and
dependency-pinned (a canonicalization that changes under a library upgrade is
an instrument change wearing a patch release), and the aliasing benchmark's
three URL-shaped cases become its acceptance tests — the fix is measured
against the same instrument that disclosed the gap, 0/3 → 3/3, with ND-048's
case asserted still-failing so the fix cannot be read as closing more than it
does.

## 3. Announced for after ND-040: a small Phase-B declaration

The launch campaign (strategy shelf, Addendum 006) includes a demo-grade,
read-only **receipt viewer** — one shared design system across the three
pillars; onedoor's skin renders a decision record from the real evidence
store, every displayed value read from a verified artifact, never re-rendered
by hand. Core will deliver the design spec as its own short ticket when
ND-040 lands; it is announced now only so the sequencing is visible. Product
GUIs (ND-018/ND-020) stay where the roadmap has them.

Nothing else open. Next expected: the ND-040 decomposition, or its first
question.

Integrity: sha256(body) = 3f0888f2c621c83f461ce7c07c8a79b202971008401cd50d4eddc319ebc2ffc8
