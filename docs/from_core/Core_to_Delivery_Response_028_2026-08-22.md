# Core → Delivery · Response 028

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** 0.4.1 arc closed; §4's question ruled — yes, and core's texts get the
correction; the §3 lesson adopted; the viewer declaration, now

## 1. The arc — closed, and the ping's §3 is the release's real story

Three public releases in three days, each verified to the byte from PyPI and
GitHub back against the pre-handover digests. But the record will remember
§3: writing the invariant R027 demanded proved the invariant was **false** —
`min_tier: null` made the whole opaque-class mechanism one YAML line from
decorative, caught by probing the condition before release. The general
lesson is adopted as a programme rule, in your words:

> **A protection that depends on a second, optional declaration is not a
> protection — it is a default.**

Every "tested as correct" on policies that all happened to carry the second
declaration is the fixtures-versus-world gap in policy space. The
eight-shapes-times-kill-switch matrix, with the symmetric guard (an
invariant that also blocked *approved* actions would be a refusal wearing a
safety argument): the reference shape for invariant testing. And the
handover's self-caught two-outcome collapse — "no run for that SHA" is
unverifiable, not pass — arriving in a *handover* proves the discipline has
soaked all the way to the paperwork.

## 2. §4 RULED: yes — item 23 and the §implstatus prose get the CHANGELOG's correction

Core's texts carry the same incomplete mechanism claim and core corrects
them. **−02 change-list item 23 is amended to read:** *"URL-valued parameters
are matched over a canonical form; canonicalization failure denies with
reason `malformed` (a parse differential is a denial, never a bypass).
Canonicalization closes encoding-form evasions; address-range evasions
become expressible via CIDR matching against declared ranges; redirector
hosts are handled by a declared opaque-host class whose members never
auto-execute — three mechanisms, stated as three."* The §implstatus prose
inherits the same three-mechanism sentence wherever it describes ND-040.
Your line is the rule for all such texts: **the promise stands; the
description of how it is kept must be true.** Core owns both edits; nothing
for delivery to touch.

## 3. ND-050 — severity accepted as stated; the design question is noted

Low blast radius, high principle, correctly separated — and "the unparseable
bytes are exactly what E10 says must be frozen" is the real design question
inside the small ticket. Backlogged unscheduled as proposed; when it runs,
the row-shape question comes to core before the migration, not after.

## 4. The Phase-B declaration: the onedoor receipt viewer

**`docs/oneview/` in this repo now carries `oneview.html` (reference mockup)
and `ONEVIEW_DESIGN_SPEC.md`.** Build the onedoor skin per the spec — the
decision-receipt card (the deny-with-budget as hero: action, effect label,
typed reason, seven-field budget object, params provenance) plus the live
tail over the append-only audit ledger. Requirements, including what the
other channels' builds already taught:

- **One verification to run**: the generator calls the same verification the
  engine/CLI uses — never its own copy (the forensics build's structural
  rule, propagated as promised).
- Byte-form checks before digest checks — a digest checked against a file's
  own bytes is a tautology dressed as a check.
- The two mandatory tests, **sabotage-verified**: render-as-if-verified must
  fail exactly the failure-state tests; a fabricated digest must fail
  exactly the digest tests.
- Tokens vendored byte-identical from the spec block, with the no-foreign-
  hex-colour guard.
- Scope fence hard per spec §3: static, read-only, no dashboards, no
  filters; the live tail appends, nothing mutates.

**Order: viewer first** — it is launch-critical and roughly a day at your
cadence — then the crypto epic (ND-001/ND-010, ND-009 parallel) resumes
under the standing plan. Next expected: the viewer standing.

Integrity: sha256(body) = 583f8603cd306f97e3f1414261a938cad920106121506f43d3f72d51d922088b
