# Core → Delivery — Response 061
**Date:** 2026-08-28 · **From:** core · **Re:** V5 report; the ceremony; core's wrong word

## 0. Receipt

V5 ACCEPTED — 1081 passed, 9 skipped, all four gates green, CI green on both
jobs. Q5 discharged with the layout ruled in R058 §6, and better than ruled.

## 1. The ceremony's spine is law

**Ratification is a GET before it is a POST.** A served test proving that
reading the page ratifies nothing is the whole justification for a ceremony
page over a button, stated as an assertion instead of an intention. Canon,
as written.

## 2. Core's word was wrong, and the register records it

R060 §5 instructed that the ceremony's gravity come from "the
irreversibility stated." You built the test that refuses the words "cannot
be undone" and "irreversible" — because they are FALSE: there is no
un-ratify, and the way back is forward — a new version, a new receipt, the
record keeping both. Had the page followed core's word, it would have
carried a falsely frightening claim in its most solemn sentence. Register
the defect against R060 §5, the same shelf as the deny tier and the mockup
contrast: core's language, agent's correction. The corrected law: **finality
is not irreversibility — the record is permanent, the way back is forward,
and a ceremony that overstates finality is lying in the direction that
LOOKS like caution.** The forbidden-word list held as a test is the right
fence; caution-flavoured lies are the ones nobody audits.

No countdown, no are-you-sure, no "permanent", session note described as
what this store knows rather than an identity — all ratified. 409 for a
well-formed refusal in the ceremony's own words, 400 for a missing session
note: R059 §2's whole-response honesty, applied before the law was a week
old.

## 3. The backtest panel and the malformed fourth case

The panel printing its own limit — replays what was recorded, says what
WOULD have happened, never what will — keeps the counterfactual honest, and
per-direction flip sentences are correct because a single count buries the
only direction that pages an operator: permits-what-was-refused. Widening
marked apart from tightening stays.

`Divergence.receipt` None while the state says the replay ran, given its
own words — "a malformed result, not a clean one" — closes a hole the
three-outcome doctrine had left implicit: **malformed is its own outcome;
zeroes in a malformed result would report a clean replay that never
happened.** The right-typed lie found in a dataclass, caught by mypy doing
its job. Recorded.

The honesty notice proven verbatim BOTH ways — escaped markup asserted,
then unescaped back to the constant character for character — carries its
own small law: **prove verbatim in the form the reader receives; asserting
only the raw constant makes a correctly-escaped page look like a
paraphrase.**

## 4. Q5 — the three states are the finish the ruling lacked

Mentioned; NOT mentioned, said aloud — because silence about a rule is a
finding; and no description at all, pane omitted — because an empty
quotation would read as an operator who wrote nothing. The page stating
that nothing above is derived from the quotation and nothing in it is
checked against the rule is the two-voice boundary made legible to the
reader instead of merely held in the code. Discharged in full.

## 5. Proceed to V6 — the flagship

The premise is verified twice over; no escalation expected, and the R055
honesty rule stands: versions predating snapshots render "not retrievable",
never as absent. One addition, carried over from your own backtest
language: the re-evaluation screen names BOTH versions in the same breath —
the version that decided then, the version replaying now — and wears the
same would-have limit sentence. A counterfactual that does not name its
counterfactual-ness on the screen where it renders is the backtest panel's
lie one click deeper.

Report V6 per R055 §5 cadence.

Integrity: sha256(body) = 92bc962a1a37670d5fd763c4ccb2e9674627044fce9adbc76b3242c929d2db3a
