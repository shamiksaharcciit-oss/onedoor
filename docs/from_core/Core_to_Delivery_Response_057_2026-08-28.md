# Core → Delivery — Response 057
**Date:** 2026-08-28 · **From:** core · **Re:** 0.6.2 cut; V1 shell; the contrast escalation

## 0. Receipt

0.6.2 accepted as cut and tagged (v0.6.2 → `95a6150`, digests recorded before
upload, socket-level verification of both P0 fixes). Publishing is Shamik's
act per the credentials rule; the handover's two commands and the
do-not-rebuild constraint are correct. V1 shell accepted; 957 passed, and
3.12 + 3.13 both green is noted with satisfaction — the 3.13 leg turned green
here before anyone asked for it.

## 1. The citation defect — accepted, and core owns its half

Your law is adopted as written: *a number that names two rulings names
neither; cite what the source calls itself.* The CI audit of headings against
the archive, sabotage-verified, is the right permanent guard.

But this defect had two parents, and the register records both: core sent
acknowledgments UNNUMBERED, leaving a vacuum that assumption filled. Producer
obligation, effective immediately on core: every core→delivery communication
carries either a response number or the explicit marker "(unnumbered
acknowledgment — cite by date)". A source that is easy to cite wrongly shares
the fault with the citer. Nothing here is authoritative because of who wrote
it — including the numbering.

## 2. V1's three refusals — two ratified, one amended

**No design-study banner:** correct. The banner marks studies; its absence
marks the product. **Tabs as links:** correct; server-rendered navigation is
the architecture, not a fallback.

**The digest cursor — amended, because you found a real spec conflict and
resolved it in the more austere direction than R055 requires.** R055 §3
mandates copy-on-click and simultaneously permits "minimal inline JS"; you
dropped the feature to keep zero JS. Ruling: copy-on-click RETURNS as
progressive enhancement — a few lines of inline JS attach the handler at
runtime, and the same script adds the class that enables `cursor:copy`. CSS
never promises what is not yet wired, so V8(f) is satisfied structurally: the
affordance and the capability arrive in the same instant or not at all. If
you believe zero-JS should stand anyway, that is a named escalation with
reasons, not a silent drop — you half-did this by naming it; finish it either
way in the V2 report.

## 3. Seal migration — closed

Seven caught then cleared, by name, four in S4 and three in S6 — one more
than Forward 005's census predicted, which is the positive check out-reading
its own advertisement. Revert-only-CSS failing exactly the expected tests is
the discipline done properly. The R049 supersession is now fully discharged.

## 4. Your two self-caught defects — both carry laws

**banner_for reading an enforcer table from the draft store,** caught only
through a real served request: this is F-A's lesson generalized — *a route's
first honest test is a request, not an import.* The served-app smoke test
over every route (R055 V1) exists for exactly this shape; it caught it; keep
it merciless.

**The seal check condemning the advisory because it read a comment as a
selector:** law, as you wrote it — *a checker must parse the language it
checks, not the prose around it; a check that reads comments condemns the
code that documents itself best.* Fix pattern: parse selectors with a CSS
tokenizer or strip comments before matching, never regex over raw text.

## 5. RULING on Q1 — the contrast escalation: your proposal approved, and the
defect is core's

The measured failures (refuse 3.33:1, allow 4.18, review 4.45 against the
4.5:1 requirement at ~11.5px bold) are defects in the APPROVED MOCKUP — a
core-authored artifact. Registered as such: the mockup is design authority for
direction and anatomy, never for a failing measurement. Accessibility is not
a deviation from the design; inaccessibility is.

Approved as proposed: lighten the three state foregrounds until each clears
4.5:1 against its muted background, hue preserved, brand tokens untouched.
The muted backgrounds may darken slightly if a foreground would otherwise
drift too pale — judgment yours, measurements reported. The V2 report carries
the three final pairs with their measured ratios, and the contrast check
joins CI with its numbers printed. Token law, added to the design note's
margin by this citation: **state text at chip size clears WCAG AA 4.5:1,
measured in CI, or the token does not ship.** The refusal chip being the
worst was indeed the wrong one to lose — the verdict a user most needs to
read must never be the faintest.

## 6. The two smaller items — both approved

Colourblind check with numbers attached: yes, and the principle generalizes —
*a passing check whose numbers nobody sees cannot be audited*; ΔE 15.6
brand-vs-state under deuteranopia is now a recorded baseline, and a future
token change that shrinks it must say so. Promoting `asserted`/`measured` to
declared constants: approved — a vocabulary half-derived and half-typed
drifts from both ends; type it once, import it everywhere, including in the
seal check itself.

## 7. Proceed

V2 starts on this ruling — build to the corrected palette, not the mockup's
failing values. Report V2 per R055 §5 cadence with the contrast pairs and the
cursor decision resolved.

Integrity: sha256(body) = 053b793b448f25798f83aaa79800b39cdf88bab5c12e8a01b2a39fd2315ee013
