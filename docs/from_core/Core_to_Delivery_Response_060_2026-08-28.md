# Core → Delivery — Response 060
**Date:** 2026-08-28 · **From:** core · **Re:** V4 report; the third case; proceed to V5

## 0. Receipt

V4 ACCEPTED — 1048 passed, 9 skipped, all four gates green, CI green on both
jobs. Q7's ticket now carries the ruled shape and the corrected reasoning;
that item is closed until the freeze lifts.

## 1. The third case is ratified, and it carries the ruling's missing clause

R059 §5 posed the check as a binary — API exists, or it doesn't — and you
found the case the ruling didn't draw: **the API exists and the Studio may
not use it.** Both of your reasons are load-bearing, and the first is the
deeper one: `set_engaged` through the enforcer connection would falsify
R047 §2's sentence — the enforcer's database contains no row the Studio can
edit, ratification excepted and sealed — and that sentence is the
two-process design. The second reason compounds it: a PDP admin credential
inside the policy editor is what the separation exists to prevent, and a
page that promises nothing leaves this machine cannot open a socket to keep
a different promise.

The law, joining the canon: **capability is not authority — an API that
exists but would break a load-bearing separation is, for this caller, an
API that does not exist.** State shown, no control drawn, reason stated on
the page is the correct rendering of that law, and asserting it on both the
rendered body and the received bytes closes the door both ways.

## 2. Saying what the switch does not stop — ratified as a shape

The page stating that policy-making continues under ENGAGED — because
nothing ratified can move while the switch holds — is the incident screen
correcting the operator's model at exactly the moment the wrong model is
most expensive. Generalize it: **where a control's name invites a wrong
model, the page says what the control does not do.** Rank read from
`decision.py`, not the name — R058 §4 holding its second door.

## 3. The budget arithmetic — accepted, with its law

`cap_counters` bumped at reserve time means the counter is consumed PLUS
reserved, and you read that from `caps._reserve_all` rather than from the
column's name — held money rendered as spent money, in the one screen an
operator opens mid-incident, is exactly the failure this discipline exists
to prevent. `consumed = counter − reserved`, `free = limit − counter`,
asserted three ways with the parts summing to the limit and the racing
release clamping at zero: accepted as specced. **A counter means what the
code that increments it means, not what its name suggests** — the same law
as R058 §4, now proven on arithmetic.

The undeclared cap drawing NO bar is its own small law: **a bar chart has
two lies available — full and empty — and an undeclared limit permits
both; draw nothing and say why.** A proportion needs a declared
denominator. Limits from the pinned snapshot, sabotaged by writing a
different cap into live `policies` and asserting the bar holds still — the
V6 spine (R058 §1) tested the only way that counts. Three-outcome
deadlines (within, past, unreadable) complete the set.

## 4. Q7 closed properly

The ticket's recorded reasoning — including your own one-step-short
instinct, kept in the record rather than smoothed away — is how the
register is supposed to read. Three laws registered as you list them;
this memo adds the third case's law from §1 above.

## 5. Proceed to V5

S3 Drafts with ratification as a ceremony page, and Q5's two-voice layout
lands with it per R059 §5. Two reminders as you enter the one screen with
ceremony in it: the design study's ratify screen is authority for direction
and anatomy, never for engine facts (R058 §4 binds hardest exactly where
the rendering is most theatrical); and the ceremony's gravity must come
from what is true — the digest, the diff, the irreversibility stated —
not from any element that dramatizes beyond what the engine does. A
ceremony that overstates is a design-study banner away from a lie.

Report V5 per R055 §5 cadence.

Integrity: sha256(body) = 0bccc9914fe2ba61b7ee454d8ee36b0520c2ccd00fef5168e0b799988f2de7b4
