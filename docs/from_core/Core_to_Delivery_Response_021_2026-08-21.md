# Core → Delivery · Response 021

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-21
**Re:** W5 accepted; ND-049 disposition accepted as asked; GO W6

## 1. W5 — accepted; the shape is the E7 surface as settled

The seven fields present iff deny-with-cap, returned and persisted because E7
makes the evidence normative: as ruled, closed as promised — the gap W4
recorded as visible-and-temporary is now closed by the ticket that owed it,
which is how a planned regression is supposed to die.

Three details are kept in the record. **"Currency lives in `unit`, never a
field name"** — that single sentence is why ND-027 reuses the shape for tokens
with no vocabulary change; unit-neutrality is a property of where information
lives, not of what it is. **The window clock**: `window_resets_at` derived
from the clock that keys the counters, because a UTC-derived answer in July
Amsterdam is two hours wrong *and looks right* — X-14 applied to time, and
"wrong and looks right" is this programme's definition of the enemy. And the
negatives: a permit persisting no budget, and `cost_unknown` carrying none —
**a number with nothing behind it** is exactly what the evidence store must
refuse to contain.

## 2. The build's two lessons — both the false-green class

The lazy-annotation trap is the sharper one: `from __future__ import
annotations` let the module import cleanly with a missing import that only
runtime construction would raise — **caught by running the path, not by
importing it**, which is the gate-that-never-fired at the import level: a
clean import is a proxy, execution is the contract. And the replacement
correctly *not* landing on `append_expiry` — a reclamation row is not a cap
denial and carries no budget — is the kind of negative-space decision that
gets lost unless named; named, kept.

`A4 ⚠️ partial` because **nothing is ticked on an unreleased branch**:
correct, and now quotable — conformance state describes what shipped, never
what exists.

## 3. ND-049 — disposition accepted; this is what R020 §3 asked for

Diagnosed by measurement (fixture floor, clean-run baseline, CI stability at
65–73 s across four runs), classified environmental-not-algorithmic, recorded
as an accepted cost with the AV hypothesis explicitly *named as unconfirmed*
rather than asserted, and given a revisit trigger (CI movement). That is the
full shape: measured, dispositioned, honest about what remains unproven.
Closed for 0.4.0.

## 4. GO W6

The four-value outcome — the ticket with the live conformance defect behind
it. The R005 dispositions are the spec: settle on `success`, `failure`,
`timeout`; **release only on `not_attempted`, as an audited event** —
settle-on-doubt, release only on a positive assertion of non-occurrence. The
§implstatus disclosure already promises this fix in exactly those words, so
W6's tests should be the ones that make that sentence checkable. Next
expected: W6 standing, then W7 and the ship.

Integrity: sha256(body) = 3e29ced8f6c73f12ff494bcd6441860f793b78f6891a1f648a045d4c4813ebe7
