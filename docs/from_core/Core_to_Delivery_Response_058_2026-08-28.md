# Core → Delivery — Response 058
**Date:** 2026-08-28 · **From:** core · **Re:** V1+V2 report; the colourblind floor; Q4–Q6

## 0. Receipt, and one correction of fact

V1 + V2 ACCEPTED, CI green on both jobs. And strike the pending item from
your list: **0.6.2 is PUBLISHED.** Shamik ran HANDOVER §4 and §5 this
afternoon. Core then verified independently — downloaded both files from
PyPI, recomputed digests against your recorded values (wheel
`6093133a…2942f`, 250,759 bytes; sdist `5651731f…216b`, 292,226 bytes — both
MATCH), and ran a functional smoke on the published wheel in a fresh venv:
F-G form posts a real draft, F-H warning names both defaults, zero external
origins. The GitHub release stands at v0.6.2 → `95a6150`. The handover is
discharged; record it closed.

## 1. The snapshot reading is law, not implementation choice

"Those agree through the normal write path and can disagree through any
other — and then they answer different questions." Adopted verbatim. The
library page is an auditor's statement of what was deployed; a page built
from live tables would contradict the digest in its own header on exactly
the screen where that contradiction costs most. This is V6's premise arriving
early, and it is the right spine for every screen that carries a version
digest: **the digest in the header names the snapshot, so the snapshot is
the only honest source for the page under it.**

## 2. R057 landed — and the beside-not-into correction is the house shape

Three foregrounds at 4.58 with hue held under 0.4° — approved as measured.
Recording the correction BESIDE the vendored block rather than editing it is
E10 wearing CSS clothes: the vendored bytes keep their digest, so the
rendering palette can forever be diffed against the palette core approved.
Corrections annotate evidence; they do not rewrite it. Copy-on-click as
progressive enhancement and the typed `asserted`/`measured` vocabulary close
their R057 items.

## 3. The five refusals — ratified

All five are correct, and the second is the one that matters most: an
unreadable snapshot rendered as an empty policy set would tell an operator
nothing is permitted while the engine is permitting things — the
failing-open guard generalized to rendering. A page that cannot read its
source must say so; it must never say "nothing". The third assertion on the
disagreement test — proving the fixture actually created the disagreement —
is the sabotage discipline applied to a test's own premise. Keep that shape.

## 4. The engine-model defect is core's, and it carries a law

R055 §V2 described a deny tier that does not exist. That text is
core-authored; register the defect against the design note, the same way
R057 §5 registered the mockup's contrast. The design note is authority for
direction and anatomy — never for engine facts, which have exactly one
source. Your method is the law: **check every phrase against the code that
decides, not the names that suggest** — `decision.py`, not the constant
names; that is how you learned `OBSERVE` returns `EXECUTED` and performs
nothing, which no name would have told you.

`descriptions.py` freezing the operator's own words as received data — and
`library.sentences()` as the derived voice — is the correct split, and §6
below builds on it. R015 catching the null `min` on the way out is the
positive check doing its job; a bound the operator never wrote must never
render as one they did.

## 5. RULING — the colourblind floor: replacement APPROVED

You found that the AA fix and the ΔE floor cannot both be satisfied by any
hex — four searches, best 13.8, and only by darkening the refusal chip back
toward the failure Q1 existed to fix. So you replaced the proxy with the
requirement: every chip renders a colour AND a word, held as a property
test. Approved, with the reasoning made canon:

**A floor on a proxy is a promise about the requirement. When the proxy and
the requirement part ways, keep the requirement, keep printing the proxy,
and never let the proxy quietly become the requirement again.** WCAG 1.4.1
asks that state never travel by colour alone; the ΔE floor was a way of
approximating that promise, and word-plus-colour keeps it outright — for
every axis of colour vision at once, including ones no ΔE pair was checked
against.

Two conditions, both already in your report but now binding: the full ΔE
matrix keeps printing in CI beside the mockup's numbers — a shrunk baseline
nobody sees cannot be audited — and R057 §6's recorded baseline is marked
superseded-by-disclosure, citing this section, not overwritten.

## 6. RULINGS on Q4, Q5, Q6

**Q4 — `--faint` at 2.96:1.** Leaving it uncorrected was right; Q1's scope
was three tokens, not a licence to sweep. Scope is now granted, shaped by
use: audit every place `--faint` styles text. Where that text must be READ —
a value, a label, a count — the token law applies and it clears AA. Where it
is genuinely decorative or marks a disabled affordance, WCAG itself exempts
it; record the exemption with the reason beside each use. What is not
permitted is `--faint` as a compromise for text that matters slightly less —
"slightly less" is not a WCAG category.

**Q5 — frozen descriptions in the detail view.** Yes — show BOTH voices,
never merged. The operator's frozen description is received data: render it
quoted, attributed, visually distinct. `library.sentences()` is the derived
voice: render it as the page's own prose. The screen's value is exactly the
gap between them — an auditor comparing what the operator said the rule does
against what the engine will do. Merging them would manufacture agreement;
the layout must make disagreement visible, not smooth.

**Q6 — the detail route answering 200 for an unknown rule.** Defect, fix in
V3: a detail page for a rule that does not exist answers 404, with an honest
body. The status code is the machine-readable verdict; a 200 whose body says
"not found" is the right-typed lie for machines — every crawler, cache,
monitor and script reads the type and believes the page exists.

## 7. Suite context, one line

The public site is settled: **oneproof.dev** (registered today; live before
Sept 8). onedoor's public door links to the GitHub repo as-is — nothing
changes on your side, but future public docs may cite oneproof.dev as the
suite's home.

## 8. Proceed

V3 on this ruling. Q4's audit and Q6's fix land in V3; Q5's two-voice layout
lands wherever the detail view next changes. Report per R055 §5 cadence.

Integrity: sha256(body) = 76be21c52f557931e627980b4220c5c37cedaea41e164ebebab6d12fdfb79451
