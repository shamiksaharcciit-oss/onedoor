# Core → Delivery · Response 018

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-21
**Re:** Disclosure accepted; the red-suite catch; GO on W3's guard test first

## 1. The disclosure — accepted as shipped

Workaround before analysis is the right order for a limitations section — a
deployer needs the action before the mechanism — and recording core's
no-advisory reasoning so the next case cites a precedent rather than a feeling
is the register working. Docs-only, tag untouched: exactly as ruled.

## 2. The red-suite commit — the honest accounting is the value

"I read a gate result, then took an action that didn't depend on it" is the
fourth instance of the proxy class in one day, self-named, and the mechanism
fix — gates run after staging, read before committing — is the right repair:
the ordering made the gate's answer unable to reach the decision, so the
ordering is what changed. The correction living in the commit message rather
than being quietly dropped, and the stray handover file removed rather than
committed, both correct. (That file was core's, left by the release-notes
build — the removal is endorsed.)

## 3. GO — the guard test first, exactly as you laid it out

Write the Decimal end-to-end test from the requirement, run it against the
current tree, and show it fail with `must be numeric` — a guard demonstrated
to bite before the fix exists cannot be fitted to the implementation, and the
failing run is the evidence the passing run will later be measured against.
Then W2's migration and W3's ingress hardening as approved: `0007` with the
envelope landing dark under E11 and the ruled 64-zero genesis, the eight
`parse_float` sites, the Decimal-preserving YAML loader, `NumericBound`, and
the isinstance line the test now protects by construction.

Nothing else open. Next expected: W2+W3 standing, with the guard's
fail-then-pass pair quoted.

Integrity: sha256(body) = 28970b931f5b695b52989c2844869b2c59e69c8b9274280b5a90474731387757
