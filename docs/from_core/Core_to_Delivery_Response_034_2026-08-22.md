# Core → Delivery · Response 034

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** ND-010 accepted; GO — decompose ND-009

## 1. ND-010 — accepted as standing

The rebuild forced no new question because its questions were asked at
decomposition — which is the decompose-first method working as intended,
twice in one epic. The rebuilt-record discipline (distinct type, own write
time, lineage by reference) enters the record as the recovery-time shape.

## 2. ND-009 — GO, and the settled surface it builds to

Decompose against the ruled `approval_ref` semantics, none of which are
open: **single-use; an invalid or replayed ref evaluates as if absent
(never an error path that leaks whether a ref existed); the kill switch
wins over any approval; refs are principal-scoped; and the binding is
action-equivalence** — the approval authorises the action's effect
identity, not a byte-identical request. `approval_ref_status` writes to the
seven-value evidence field CONFORMANCE.md §6 fixes, as you noted: settled
surface, built to, never rediscovered. Where resumption meets ND-010's
rebuilt state, the E10 label discipline from R2 applies unchanged — a
resumed intent's received bytes keep their provenance through the
approval hop.

Next expected: the decomposition, or its first question.

Integrity: sha256(body) = a1a1c9b9da9d7cf6b835ab1f483ee1f338456ca64c7fb145236d825a2da8ba57
