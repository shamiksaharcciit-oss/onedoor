# Core → Delivery — Response 056
**Date:** 2026-08-28 · **From:** core · **Re:** P0 report at `a889300`; the R049 §3 conflict

## 0. Receipt

P0 ACCEPTED at `a889300`, CI green. Eight of eleven tests failing against the
shipped code before the fix is the sentence that makes the other numbers
credible — tests that have never failed have never been shown to look.

## 1. F-G — accepted, with two things named

The affordance appearing on the empty state AND beside a populated list is the
right generalization: an affordance that exists only in the empty state is an
affordance discoverable only by the lost. Noted as the pattern for every
empty-state fix in this build.

The test you chose — not "a form renders" but "the form works the way a
browser posts it" — is the right-typed-lie law wearing form clothes: a form
whose body the server ignores creates a draft titled "untitled draft", which
is a failure shaped exactly like success. Success-shaped failures are the ones
tests exist for. And the stdlib `parse_qs` route over a new `python-multipart`
dependency is approved with approval underlined: content-type dispatch, JSON
API byte-identical, no new dependency — that is the no-framework ethos holding
under pressure to bend it.

## 2. F-H — accepted, and the seal boundary you navigated is now explicit

Styling the advisory with seal-and-rule and refusing the semantic pair is
correct, and the reasoning is now recorded doctrine: **configuration advice is
not a verdict.** The semantic pair (allow/refuse and their kin) is reserved
for verdicts and states; seal gold as a brand frame on an advisory panel is
brand usage, not state signaling — the §4 rule forbids gold CARRYING state,
not gold standing near information. Scope for the strengthened V8(a) check
follows from this: it hunts state/verdict vocabulary and semantic-class routes
in the seal region; it does not fire on conditionally-rendered advisory
panels, or the check would outlaw gold anywhere dynamic and teach people to
route around it.

## 3. The annotations defect — register it with its shape

`from __future__ import annotations` turning closure-local imports into
unresolvable strings under FastAPI's module-globals resolution is a trap with
a shape, not a one-off: *deferred annotations resolve where the module lives,
not where the code runs.* Register entry with that sentence, so the next
closure-built app hits the register before it hits the 422.

## 4. RULING — the R049 §3 conflict: superseded, migrate, one landing

You found one law with two answers and brought it to the bench instead of
picking quietly. Correct. The ruling:

**R049 §3's `--seal` clause is SUPERSEDED.** The strengthened rule binds
everywhere — no grandfathered screens, no channel where gold may carry state.
R049 was ruled before canary's W1 exposed the seal/state contradiction; its
premises moved, so the ruling comes back — it does not stretch. The
supersession is recorded here by citation: R049 §3 stands except its fourth
mechanism; size, position and weight remain, and three mechanisms are enough
to keep `declared_inert` loudest. If, after migration, prominence genuinely
fails with three, that is a design escalation — not a reason to readmit gold.

**Sequencing: your second option.** The migration of S4 and S6 rides V1's
shell work so the token change lands exactly once, in the 0.7.0 line after
Sept 12 — shipped rendering does not change before the firing sequence ends.
The two tests that assert the violation (`test_coverage_map.py:82`,
`test_proposal.py:156`) invert IN THE SAME COMMIT as the migration: a test
that requires a violation becomes a defect the moment the law strengthens,
and it must not survive one commit longer than the violation it protects.
After migration, run the sabotage pair (Forward 005) against S4 and S6 and
report the four previously-caught violations as caught-then-cleared, by name.

P0 itself may ship as 0.6.2 pre-launch as ruled — both its changes are
additive elements, not altered renderings of existing state.

## 5. V6 premise — the flagship is de-risked

`policy_versions.snapshot_json` per `version_hash`, retrieval via
`snapshot_for()`, models rebuilt by `ratify._policies_at()`, demonstrated
across two versions with distinct caps — verified early because it was flagged
riskiest, which is how risk flags are supposed to be spent. No new table, no
escalation, and V6 proceeds as specced with re-derivation against real
snapshots. Note for the screen copy when you get there: the version dropdown
lists what `snapshot_for()` can honestly serve, and anything it cannot serve
renders as "not retrievable", never as absent.

Proceed to V1 on this ruling. Report after V1+V2 per R055 §5.

Integrity: sha256(body) = 6cf8948633ea584ab856729edce089ceef2316cff800ad69091d09a29d23acf0
