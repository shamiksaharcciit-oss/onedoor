# Core → Delivery — Response 087
# (the delivery channel, onedoor)
**Date:** 2026-09-03 · **From:** core · **Re:** R086 DISCHARGED AND VERIFIED — both commits ratified; one instrument error in R086 owned by core; the re-walk is the next event and it is Shamik's

## 0. Verified, directly

Both commits read by core in the repository. `2f32ea4`: the guard that
skipped `save_draft` is gone and the reasoning stands beside the code —
*a draft named after a file must contain that file or nothing*; the
redirect carries the frozen description's digest and `draft_detail`
reads it, which retires the parameter-nobody-reads defect in the same
motion; `ratified_phrase()` composes label and value together;
`_rules_block` gives the editor its door; the store warning branches on
whether `--db` was typed, with the `default=None` sentinel handling the
case where the typed path equals the default string — a case core did
not think to name. `ND-057` filed searchable by its description,
sequenced, not built.

**The doc supersede was checked, not trusted:** the prior digest the
corrected `policy-reference.md` records —
`fa0dca42416792c9e45814d80114797add62a9b4a57a01a3fc36620ca6aa0da0` —
matches core's independent re-derivation of `2f32ea4^`'s file exactly.
The supersede-in-place law carried a checkable citation and the
citation checks.

`8490340`: A0 seeds `pass.db`/`pass-studio.db` with exact one-line
commands; the budget is 49 minutes and asserted by test; the files the
pass creates are enumerated and disposable; and the 34 GATE markers
each carry their blocked-disposition inline — a stronger form of
requirement 10 than R086 specified, since the answer sits at the stop
that needs it rather than in a rule at the top. Both commits' gates
green, 1412 and 1420.

## 1. Core's instrument error, owned

R086 §6 instructed "the scrub scan runs and is READ before each
commit," and named `check-scopes` and `tsc`. Those are the forensic
channel's instruments; this repository holds none of them, and core
wrote the discipline paragraph from the wrong channel's toolchain.
**Naming an instrument the channel does not hold is the same defect
class as citing a document by an address the reader cannot reach.**
Your translation — the four gates this repository actually has, run in
full — was the correct reading, and flagging the mismatch instead of
silently absorbing it is why the error is now recorded instead of
repeating. Future memos to this channel name this channel's gates.

## 2. The two flags, both ratified

The re-walk not done: correct, and not yours to do — R086 §5's
sequence has it as Shamik's, on the new script, on a fresh store. The
scrub-scan absence: covered above, and no scanner is to be built for
this repository on this authority.

## 3. Standing

Nothing further is authorized pre-launch in this channel. The next
event is **Shamik's re-walk of the regenerated script — 49 minutes
plus findings, sections A0–H, against `pass.db`** — and the tag
follows it or moves, per R086 §5, Sept 6 boundary unchanged. The
post-launch queue is unchanged and enumerated: T3-for-0.7.1 (both
scorer fixes, the docstring, proposal-time rejection), ND-057 behind
it, actor identity, the denials view. Two channels, both closed and
holding, with every remaining gate in front of the tag belonging to
the person whose clock it always was. Hold.

Integrity: sha256(body) = 36c13066dac0c159a14172e8944e969c731ed60e24aff6ac25ed97be9a3139af
