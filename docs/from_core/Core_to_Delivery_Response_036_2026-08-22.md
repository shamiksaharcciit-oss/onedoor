# Core → Delivery · Response 036

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** ND-052 RE-SEQUENCED by Shamik's decision — the Policy Studio becomes
a pre-launch epic, starting the moment the crypto epic lands

## 1. The decision, recorded as what it is

Shamik, wearing the business hat, has re-sequenced ND-052: **the Studio is
wanted for the launch** — demo-grade, in parallel with the forensics
product's final phase. This supersedes the design note's "after the epic, no
code before launch" line by principal's decision, recorded as such. The
five-principle constitution is unchanged and binds every sub-ticket exactly
as written; what changed is only *when*.

## 2. Build order — the deterministic spine first, the model last

This ordering is now normative for the epic, and launch pressure is the
reason it exists rather than a reason to skip it:

- **S1 — the backtest engine.** Replay a candidate policy against the
  actual audit ledger (dry-run, deterministic, zero LLM): "under this
  policy, yesterday's N actions: X allowed, Y to approval, Z denied — here
  they are." Leans on machinery that now exists; independently demoable.
- **S2 — the ratification ceremony.** Diff against the active policy, the
  canonical form and its hash shown becoming `version_hash`, the derivation
  receipt issued on signing. The Oneview seal, stamped.
- **S3 — the policy canvas.** Rule cards: plain-English meaning *derived
  from the canonical artifact* (principle 2), canonical form on flip, dials
  inline, fail-closed defaults (principle 3).
- **S4 — the coverage map.** Mentioned / covered / uncovered, the honest gap
  rendered prominently (principle 4).
- **S5 — one vertical template pack** (finance/payments — matching the demo
  incident), the grounding substrate.
- **S6 — the LLM proposer, LAST.** Drafts against S5's templates, emits a
  candidate document and nothing else, structurally outside the decision
  path (principle 1), the derivation receipted (principle 5). It lands on a
  proven spine, not the spine on it.

## 3. Two gates that keep the wow honest

**The launch-gate rule: the Studio never gates the launch.** If any sub-
ticket slips, launch ships without it and the vision slide stands in — the
Studio is launch-enhancing, never launch-blocking, and no date moves for it.

**The demo bar vs the ship bar.** The design note's §5 (principles enforced
structurally or it does not ship) remains the *product* bar. The *launch
demo* bar: S1–S4 live on real artifacts; S6 may demo only if its output is
real, its derivation receipted, and its limits stated on camera — per the
house law, nothing narrated ahead of its measurement. A Studio demo that
fakes a proposal would cost more than the Studio earns.

## 4. Sequencing

Finish the crypto epic first — ND-009, ND-015, ND-017 as ruled; the Studio
epic opens the day ND-017 closes. ND-050 stays backlogged. Next expected:
the epic's close, then S1's standing or the first question the backtest
forces.

Integrity: sha256(body) = 3fa5b06ae89bbc35c3508eaa00b8cc968339891d639293941f9922dce9013a5d
