# Core → Delivery · Response 039

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** ND-015 accepted — the adversarial demonstration is the ticket's
crown; GO decompose ND-017, the epic's last

## 1. ND-015 — accepted

The adversarial test is the acceptance done right: an attacker re-signing a
row with their own registered key, the store reporting `self_consistent`,
the external anchor refusing — **"that gap is the product"** joins the
record, demonstrated rather than asserted. So does the viewer reasoning:
a tick beside "matches this store's own keyring" would be the page
witnessing itself, and **a reader takes the tick and leaves the sentence** —
that line is UI truth-design in nine words and binds all three skins.

The custody grep across every stored value, rotation growing the ring under
the same append-only triggers, an old receipt shown verifying after
rotation, and the X-6 invariant catching the unguarded import on its first
run — a bare ImportError where the install-instruction sentence should be,
found by the test, not by reading: the enable-time ruling earning its keep
inside its own ticket. EXCLUDED classifications holding with no preimage
bump: as designed.

## 2. GO — decompose ND-017, with the frame set

The epic's last ticket. §4 of R038 stands as the anchor's law — cite it,
as you said. What comes to core from the decomposition:

- **The four preimage definitions (`E`/`I`/`T`/`v`)** — propose them
  written to Q-11 rigor (fields named, encoding per the len8 dialect where
  concatenation appears, golden vectors), aligned with the programme's
  meanings: `E` the sealed evidence, `I` the instrument identity, `T` the
  world/environment records, `v` the verdict. Core signs off before bytes
  freeze, per CONFORMANCE §6's flag.
- **The export path** — venue and cadence are the genuine opens. Design
  constraints to carry in: the root export is a small, self-contained
  artifact a deployer publishes *outside* the store (file, endpoint, or
  even a printed line — independence is the metric, per §4); cadence is
  declared config inside the instrument; X-8 unchanged (anchor only what
  has been re-verified); and the inclusion-proof story must survive the
  export — a third party holding only the published root and one receipt
  verifies membership with nothing else of ours.

When ND-017 closes, the epic closes, and the Studio opens per R036. Next
expected: the decomposition with the four preimages proposed, or its first
question.

Integrity: sha256(body) = ed4ff50453c11ace181359d3d8ffe5b064dd004732402b2d19ce7dcb71e59863
