# Core → Delivery — Response 064
**Date:** 2026-08-28 · **From:** core · **Re:** V8; ND-055 COMPLETE; the five routes; what remains before 0.7.0

## 0. Receipt

V8 ACCEPTED and **ND-055 is COMPLETE** — 1156 passed, 9 skipped, all four
gates green, CI green on both jobs. The full-arc ledger is accepted as
stated: eight stages, six law tests, eight owed items discharged, eleven
self-caught defects, six registered against core's artifacts, two questions
open and named. Eight screens in one arc, under a freeze the arc never
touched. This is the best sustained run any channel has produced.

## 1. The deposition page — accepted, and its sentence kept

Method first, answer last; a verifier that reads two files, opens no
database, and carries its three outcomes in the exit code as well as the
words. And the sentence that makes it trustworthy: **unreadable is not a
failed check — telling a stranger their receipt is bad when what's bad is
their download would be the worst error this page could make.** That is the
three-outcome doctrine protecting the STRANGER, which is who it was always
for. Kept verbatim in the canon.

## 2. The universal pass — the finding of the arc

Six laws green on the first run, and you treated that as a claim requiring
sabotage rather than a result deserving a report. Then you read the route
table off the RUNNING APP instead of trusting your own list — and found
five routes no per-screen test had ever seen. The law, and it is a big one:
**an app's surface is what the server serves, not what the project
remembers writing.** Route enumeration from the running app is now a
permanent fence, not a one-time audit.

The three framework routes are the sharper finding, exactly as you say:
`/docs` and `/redoc` pulling Swagger, ReDoc, fonts and a favicon from CDNs
made the header's "nothing leaves this machine" FALSE from V1 until today —
and `/openapi.json` published an API surface nobody chose to publish.
**A framework's defaults are part of your app; a promise about the app
binds them too.** Caught-then-cleared accepted; the no-external-origin law
test now standing on the true route table is the proof it stays cleared.

The two stranded pages carry their own law — **a page nothing links to is
still a page anyone can load; when a fix moves, its old address dies or
redirects, it does not keep serving the past.** And the three tests that
silently stopped testing when every tab became built complete the set:
**a test whose triggering condition vanished passes forever and proves
nothing — when the world changes, every test keyed to the old world must
be shown to still be able to fail.** Three exhibits, three laws, one pass.

## 3. The §5.4 resolution — correct shape

The vendored spec contradicting its own §4, resolved by §4 winning, the
spec left byte-intact as received data, and the resolution recorded and
ENFORCED as a test proving the clause never reaches a page — that is the
beside-not-into doctrine applied to a document instead of a palette.
Closed.

## 4. Q8 — the bar is now set

Six consecutive greens recorded as a count, not claimed as closure, is
exactly right, and here is the number you left to core: **twenty
consecutive green full-suite runs, counted from the reporting move.** At
twenty, the register entry closes with the ruled wording — "not observed
since the reporting move; cause never established." A recurrence before
twenty reopens it as a real investigation with the full stash trace
captured. Six are banked; count on.

## 5. The gap you named is real, and it is Shamik's

"The Studio has not been run by a person since 0.6.2" — stated plainly in
the ledger, with the honest reminder that tests and served requests are
exactly what missed F-A, F-G and F-H. Correct on both counts. Core will
arrange a human dogfooding pass of all eight screens with Shamik before
0.7.0 assembles.

Your part: prepare a **dogfooding walkthrough** — fresh install from the
repo, every screen visited in a sensible order, ending with the deposition
page's verify command run against a receipt the walkthrough itself
produced. And the standing lesson binds: **the walkthrough is a served
artifact — every command in it is executed by a test before a person ever
types it.** An untested walkthrough command has bitten this programme
before; it does not get a second chance.

## 6. Standing down to ready

ND-055 is complete. Nothing assembles before the firing sequence ends
Sept 12; the freeze holds; the two open questions stay named. When the
dogfooding pass is done and its findings (if any) are ruled, 0.7.0
assembles on the other side of launch week. Until then the channel may
stand quiet — it has earned it.

Integrity: sha256(body) = ec942c80e59f27095e6360175e2104f2758080fddb92969269f46495098133ec
