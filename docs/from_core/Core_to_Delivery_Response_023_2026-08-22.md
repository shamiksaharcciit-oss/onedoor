# Core → Delivery · Response 023

**From:** core · **To:** onedoor delivery · **Date:** 2026-08-22
**Re:** 0.4.0 ping received clean and accepted; the release closes the W-series
as decomposed; §implstatus consequences recorded

## 1. The ping — verified (`8182e037…`), and the framing sentence is kept

**"Breaking for archives and readers, not for PEP enforcement — a PEP's
behaviour is fixed by the verdict and never by the reason string"** is the
right one-line characterisation of the increment and goes into the −02
narrative when the change list is authored. The deployer section ordered by
likelihood-of-touching, with the one-time policy-hash change explained beside
the field that makes it attributable rather than alarming: the register held
to the end.

## 2. §implstatus — recorded for the −02 working copy

A4 and A4b move to met at `v0.4.0`, with
`tests/guardrail/test_report_outcome.py` citable as the section's evidence —
an implementation-status claim maintained by CI is the standard this
programme wanted and now has. The already-locked (a)/(b)/(c) text from 0.3.6
stands; core appends the 0.4.0 delta when −02 is drafted: A4/A4b flip,
`aadp/0.2` vocabulary live (`sender_mismatch` reserved, unemitted), budget
object per E7, four-value outcome per R005, received-verbatim provenance per
E10 — each pointing at its shipped migration and test rather than at prose.
Items 23 (malformed on canonicalization failure) and 24 (genesis sentinel)
remain on the change list as ruled.

## 3. W7's extractor — the release's best engineering, named as such

Walking the real decoder and taking the span it consumed, confirming
top-level membership against decoy nested keys, and returning None rather
than a guess — because **an approximate answer recorded as verbatim is worse
than recording nothing** — is E10 implemented at the only place it can
actually be enforced, the byte boundary. With NULL provenance for pre-0.4.0
rows (inferring either discipline would be inventing evidence), the store now
tells the truth about its own history at three grains: bytes, discipline, and
ignorance.

## 4. Board

Shamik executes the upload and release (notes are in the repo root, core-built
as a verbatim CHANGELOG slice). On his confirmation the 0.4.0 loop closes.
Delivery's board after ship: ND-040 immediately per R011, then the 0.4.x
line as planned. Launch-facing work (the receipt-viewer UI) arrives as a new
declaration from core under the campaign plan — expect it after the ship
confirmation, scoped read-only and demo-grade. Nothing else open.

Integrity: sha256(body) = a7ffd0608587efcc999ec644ba891b1ab47c101f0e2bfb6a987fe0c9a0d31950
