# Handover — publishing `onedoor 0.5.0`

**For Shamik.** Everything below is prepared and verified; the two commands that need
your credentials are in §3 and §4. Run them from the repo root.

**Tag:** `v0.5.0` @ `fef596edf90ecdd65a39b49cef934b328274fd6d` — pushed. CI green on both jobs.

---

## 1. What is being published, and what it claims

Additive. No wire-observable change — no new reason codes, no changed verdict shapes, no
altered two-phase exchange, and a `-00` enforcement point is unaffected. Seven
forward-only migrations (`0012`–`0018`) apply on first run.

**Why this release is being cut now** (R048 §3): the crypto epic is the launch's proof
pillar and it was sitting unreleased. *A claim demonstrated from an unreleased branch is
a claim; a claim demonstrated from a published wheel is evidence.* After this, every
demo and receipt shown at launch is reproducible by a stranger running
`pip install onedoor==0.5.0` and nothing else.

The Policy Studio ships behind its `[studio]` extra and the notes **name its boundary
exactly**: backtest engine, ratification ceremony, canvas — the coverage map, finance
pack and proposer are **not** included.

Notes: [RELEASE_NOTES_v0.5.0.md](RELEASE_NOTES_v0.5.0.md), generated as a verbatim slice
of [CHANGELOG.md](CHANGELOG.md)'s `0.5.0` section.

## 2. Verification already done (so you do not have to)

| Check | Result |
|---|---|
| `ruff check .` | `All checks passed!` |
| `ruff format --check .` | `132 files already formatted` |
| `mypy onedoor` | `Success: no issues found in 60 source files` |
| `pytest -q` | `719 passed, 9 skipped` |
| CI on `fef596e` | `success` — `py3.12` and `py3.13` |
| `python -m twine check dist/*` | `PASSED` on both artifacts |
| Migrations in the wheel | **18**, `0001`–`0018`, plus the vendored `ONEVIEW_DESIGN_SPEC.md` |
| Clean-venv install (wheel only) | `Database.init()` applied all 18; a real `decide_and_reserve` returned `PermittedIntent`; the row sealed at `seq 1`; `verify_decision` gave `chain`/`params_byte_form`/`policy_snapshot`/`append_only` **verified** with `signature`/`anchor` **absent** |
| `python -m onedoor.viewer --demo-store` | wrote an 11 KB page, from the installed wheel alone |
| `python -m onedoor.studio --host 0.0.0.0` | **exit 2** with the bind refusal |
| `python -m onedoor.studio --host 127.0.0.1` | bound and served until killed (exit 124 from `timeout`) |

The last two were read **without a pipe**: `$?` after `| tail` is `tail`'s status, and the
first reading of it said `exit=0` for a command that had actually exited 2.

**Artifacts** (digests generated by tooling, never transcribed — X-11):

```
onedoor-0.5.0-py3-none-any.whl  214812 bytes
  sha256 e770ad0028a97e44cc4f40cb9eec12b5551d11cd603b4bed1d5490b8019e5cba
onedoor-0.5.0.tar.gz            240591 bytes
  sha256 e597b11c54a44aa85188319d116f0637c794dc1e496d58ac4384231ea806cd44
```

## 3. Publish to PyPI — your credentials

**Shell: PowerShell**, which is what this machine opens. Every command below is a
**single line with no continuations**, so it pastes correctly into PowerShell *and* into
bash — see the note under §4 for why that is now the rule.

```powershell
cd C:\Users\polo2\Downloads\onedoor
python -m twine upload dist/onedoor-0.5.0-py3-none-any.whl dist/onedoor-0.5.0.tar.gz
```

Both files explicitly, rather than `dist/*`, so a stale artifact left in `dist/` from an
earlier build cannot be uploaded by a glob. If `dist/` has anything other than those two
files in it, stop and tell me.

## 4. Create the GitHub release — same motion as the tag (R011)

```powershell
cd C:\Users\polo2\Downloads\onedoor
gh release create v0.5.0 dist/onedoor-0.5.0-py3-none-any.whl dist/onedoor-0.5.0.tar.gz --title "onedoor 0.5.0" --notes-file RELEASE_NOTES_v0.5.0.md
```

> **Corrected after the fact, and recorded rather than quietly patched.** The draft of
> this handover that reached Shamik wrote this command with bash `\` line continuations.
> **PowerShell 5.1 does not parse those** — it would have broken at the first line break.
> Handovers execute on Windows, so the rule is now: **write the commands in the shell
> they will actually run in, and state which shell that is.** Single-line commands satisfy
> both shells at once, which is why every command here is one line. A command that cannot
> be pasted is not a handover.

`--notes-file` rather than pasted text: the notes are a verbatim slice of the changelog
and stay that way. **Do not** backfill releases onto older tags — a retroactive release
carries a date that misstates when the artifact was published.

## 5. After you run them — what to send back

1. The PyPI URL, and
2. the `gh release create` output URL.

I will then verify the publication the way `0.4.0` and `0.4.1` were verified — install
`onedoor==0.5.0` from PyPI into a fresh venv, exercise `Database.init()` and a real
decision, confirm both artifacts are attached to the release — and write the `0.5.0` ping
to core.
