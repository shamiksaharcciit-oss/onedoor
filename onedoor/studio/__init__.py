"""The Policy Studio (ND-052). Pre-launch, demo-grade.

**The proposer is never the enforcer.** Nothing in this package writes to
`actions_audit`, and the one place that could — the backtest — cites the sealed chain
instead. *A backtest proves it saw real data by citation, not by writing; the ledger
vouches for the backtest, never the reverse.*
"""
